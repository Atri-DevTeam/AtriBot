name: Build and Release

on:
  push:
    branches:
      - AtriMeow

jobs:
  build-and-release:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: '24'
          cache: npm
          cache-dependency-path: |
            webui/package-lock.json
            miniapp/package-lock.json

      - name: Install WebUI dependencies
        working-directory: webui
        run: npm ci

      - name: Build WebUI
        working-directory: webui
        run: npm run build

      - name: Set up Java 25
        uses: actions/setup-java@v6.0.0
        with:
          java-version: '25'
          distribution: 'temurin'

      - name: Install Miniapp dependencies
        working-directory: miniapp
        run: npm ci

      - name: Test and build Miniapp
        working-directory: miniapp
        run: npm test && npm run build

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v6
        with:
          cache-read-only: false

      - name: Make Gradlew executable
        run: chmod +x gradlew

      - name: Test Miniapp authentication
        run: ./gradlew test --tests 'top.yzljc.atribot.miniapp.*' --no-daemon

      - name: Get project version
        id: get-version
        run: |
          # 使用 Gradle 命令获取版本
          version=$(./gradlew properties -q | grep "^version:" | awk '{print $2}')
          echo "version=$version" >> $GITHUB_OUTPUT

      - name: Build with Gradle
        run: ./gradlew build -x test --no-daemon

      - name: Create or update GitHub Release
        uses: softprops/action-gh-release@v3
        with:
          tag_name: v${{ steps.get-version.outputs.version }}
          name: Release v${{ steps.get-version.outputs.version }}
          files: build/libs/*-${{ steps.get-version.outputs.version }}.jar
          make_latest: true
